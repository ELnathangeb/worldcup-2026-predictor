# Requirements -- FIFA World Cup 2026 Predictor (Streamlit Community Cloud)
#
# MODEL PICKLE COMPATIBILITY
#   best_model.pkl was serialised with numpy 2.0.2, scikit-learn 1.6.1,
#   lightgbm 4.6.0. Loading with older major/minor versions raises
#   InconsistentVersionWarning and may fail. Do NOT downgrade these three
#   packages without retraining the model.
#
# Dev-only packages intentionally excluded:
#   jupyter, ipykernel  -- Qt deps break Cloud builds
#   pytest, pytest-cov  -- testing only
#   xgboost             -- training only
#   beautifulsoup4, lxml -- data collection only

# Model runtime (exact pins -- pickle compatibility)
numpy==2.0.2
scikit-learn==1.6.1
scipy==1.13.1
lightgbm==4.6.0
joblib==1.4.2

# Data handling
pandas==2.2.3

# Dashboard
streamlit==1.44.0

# Visualisation
plotly==5.24.1
matplotlib==3.9.2
seaborn==0.13.2
statsmodels==0.14.4

# Utilities
python-dotenv==1.0.1
tqdm==4.66.4
pyyaml==6.0.2
requests==2.31.0
